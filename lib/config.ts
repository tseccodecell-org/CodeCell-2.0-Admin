// all api calls go through next.config.ts rewrites to the go backend (:8000) —
// nothing in this app talks to the judge engine directly
export const TOKEN_KEY = 'cell_admin_token'
export const ADMIN_TOKEN = 'admin123'
